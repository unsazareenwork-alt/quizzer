#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../opencollective-postinstall/index.js" "$@"
else
  exec node  "$basedir/../../../opencollective-postinstall/index.js" "$@"
fi
# cmd-shim-target=C:/Users/unsaz/OneDrive/Desktop/new quiz app/node_modules/.pnpm/tesseract.js@7.0.0/node_modules/opencollective-postinstall/index.js
